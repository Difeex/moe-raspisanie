# Служебные файлы macOS
.DS_Store

# Временные файлы Excel, которые появляются рядом с открытой книгой
~$*

# Тестовые данные — реальная выгрузка с ФИО, группами и аудиториями, а также
# фотография легенды корпусов. Репозиторий публичный: этому здесь не место.
# Файлы остаются на диске и используются при проверках локально.
docs/

# Карта проекта для ассистента — рабочий файл, не часть приложения
CLAUDE.md

# Генератор записей входа. Механика выдачи доступов — не часть публикуемого
# приложения: секретов в самой утилите нет, но и лежать в публичном
# репозитории ей незачем. Файл остаётся на диске и открывается двойным кликом.
tools/
